model Wallet {
    id                String      @id @default(uuid())
    profileId         String
    name              String?     @default("No name set yet")
    network           String
    walletAddress     String
}