<b><%= LocaleUtils.getUserProfile(i18n, "title") %></b>

<%= LocaleUtils.getUserProfile(i18n, "name", name) %>

<%= LocaleUtils.getUserProfile(i18n, "email", profile.email) %>

<%= LocaleUtils.getUserProfile(i18n, "status", profile.status) %>

<%= LocaleUtils.getUserProfile(i18n, "walletAddress", profile.walletAddress) %>